{
  "name": "@xs/shared",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "dev": "vitest",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "vitest": {
    "environment": "node"
  },
  "dependencies": {},
  "devDependencies": {
    "msw": "^2.4.9",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
