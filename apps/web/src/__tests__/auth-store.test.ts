import { describe, test } from "vitest";
import { useAuthStore } from "../lib/auth-store";

describe("auth store", () => {
  test.todo("test_store_sets_auth");
  test.todo("test_store_clears_auth");

  void useAuthStore;
});
